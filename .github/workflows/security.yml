name: Security

on:
  pull_request:
    branches:
      - dev
      - master
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  scan:
    name: Scan
    uses: skillstruck/workflows/.github/workflows/security-scan.yml@master